# Railway Configuration
[build]
builder = "nixpacks"
buildCommand = "cd backend && npm ci && npx prisma generate --schema=./prisma/schema.prisma && npm run build"

[deploy]
startCommand = "cd backend && npx prisma migrate deploy --schema=./prisma/schema.prisma && npm run start"

[env]
NODE_ENV = "production"
